**flask** 
config模块，提供多种配置加载存储方式，环境变量、对象、字典映射
继承自内置模块dict，以自身作为键值存储配置值。提供一系列加载配置的方法，通过环境变量读取from_envvar();
from_prefixed_env同样是环境变量读取只是加了一个前缀匹配；
from_object 来自对象
from_mapping mapping映射（dict或者关键字参数）
get_namespace 获取指定前缀的命名空间字典

**falsk中的类型检查时的写法**

```
if t.TYPE_CHECKING:  # pragma: no cover
    from _typeshed.wsgi import WSGIEnvironment
    from werkzeug.test import TestResponse

    from .app import Flask
```

​	目的：1、有些地方引用了其中的某些库做类型检查。 2、避免运行时的循环导入。3、减少运行时的不必要开销

#### importlib.metadata` 概述

`importlib.metadata` 是 **Python 标准库**（Python 3.8+）中的一个模块，用于**访问已安装包的元数据**（metadata），例如版本号、作者、许可证、入口点等信息。

